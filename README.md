# Harness SDK for Java

This SDK allows to enrich a test suite with information on how tests are to be executed in the context of a generated call graph. This allows to provide information that the Harness agent cannot resolve automatically.
